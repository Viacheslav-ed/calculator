const calculate = () => {
  // const { msrp, tradeIn, downPayment, creditScoreValue, apr, term, mileage } = data;
  // const res = {};
  // res.lean = ((msrp - tradeIn - downPayment) * creditScoreValue * apr) / term;
  // res.lease = ((msrp - tradeIn - downPayment) * mileage * creditScoreValue) / (10000 * term);
  return Promise.resolve('123');
};

export default calculate;
